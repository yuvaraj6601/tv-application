// Add all asset imports here. Never import assets directly in screens or components.
export const Assets = {
  // example: logoImage: new URL('../assets/images/common/logo.png', import.meta.url).href,
};
